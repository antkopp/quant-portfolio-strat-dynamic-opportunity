[variables]
GITHUB_TOKEN = "${{GITHUB_TOKEN}}"

[phases.install]
cmds = [
  "git config --global url.\"https://$GITHUB_TOKEN@github.com/\".insteadOf \"https://github.com/\" && python -m venv --copies /opt/venv && . /opt/venv/bin/activate && pip install -r requirements.txt"
]
